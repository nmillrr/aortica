# Progress Log

## Learnings
(Patterns discovered during implementation)
- Python 3.13 does not have PyTorch or neurokit2 available via pip. Made these optional dependencies (`[torch]`, `[signal]` extras).
- ECGRecord uses scipy.signal.resample (Fourier method) for resampling; scipy is already a core dependency.
- to_millivolts() supports µV, uV (ASCII), mV, and V unit strings.
- Use `setuptools.packages.find` with `include = ["aortica*"]` to auto-discover subpackages.

---

## Iteration 1 - US-001: Project Scaffolding and Repository Structure
- Implemented: Full project scaffolding with pyproject.toml, Apache 2.0 LICENSE, README.md with CI badge
- Files changed:
  - pyproject.toml (new) - project config with deps, mypy, ruff, pytest settings
  - LICENSE (new) - Apache 2.0
  - README.md (new, overwrote placeholder) - project overview, install instructions, structure
  - .gitignore (new) - comprehensive Python/ML ignores
  - aortica/__init__.py (new) - root package with version
  - aortica/{io,signal,models,xai,evaluation,data,utils}/__init__.py (new) - subpackage inits
  - aortica/io/ecg_record.py (new) - minimal ECGRecord dataclass placeholder for conftest
  - tests/conftest.py (new) - shared fixtures
  - tests/{__init__,io/__init__,signal/__init__,models/__init__,xai/__init__,evaluation/__init__,data/__init__,utils/__init__}.py (new) - test package structure
  - tests/test_scaffolding.py (new) - smoke tests for package structure
- Learnings for future iterations:
  - torch and neurokit2 are optional extras due to Python 3.13 incompatibility
  - ECGRecord is already stubbed in aortica/io/ecg_record.py — US-003 should flesh it out
  - mypy configured with ignore_missing_imports=true for optional deps
---

## Iteration 2 - US-002: CI/CD Pipeline Setup
- Implemented: GitHub Actions CI workflow (.github/workflows/ci.yml)
- Files changed:
  - .github/workflows/ci.yml (new) - CI workflow with ruff, mypy, pytest across Python 3.10–3.12
  - tests/test_scaffolding.py (modified) - auto-fixed import sorting via ruff --fix
- Learnings for future iterations:
  - README already had CI badge from US-001, pointing to `.github/workflows/ci.yml`
  - ruff auto-fix (`ruff check --fix`) handles import sorting issues cleanly
  - CI matrix tests Python 3.10, 3.11, 3.12 — only installs `.[dev]` extras (no torch/tf/signal in CI)
---

## Iteration 3 - US-003: Canonical ECG Data Representation
- Implemented: Full ECGRecord dataclass with resample(), select_leads(), to_millivolts() methods
- Files changed:
  - aortica/io/ecg_record.py (modified) - added resample, select_leads, to_millivolts methods; uses scipy.signal.resample
  - tests/io/test_ecg_record.py (new) - 30 tests covering construction, validation, properties, resample, select_leads, to_millivolts
- Learnings for future iterations:
  - ECGRecord.resample uses scipy.signal.resample (Fourier method) — already a core dep
  - to_millivolts() handles µV, uV (ASCII fallback), mV, V
  - All utility methods return new ECGRecord copies, never mutate in place
---

## Iteration 4 - US-004: WFDB Format Reader
- Implemented: `aortica.io.read_wfdb(path)` function that loads WFDB .hea/.dat files into ECGRecord
- Files changed:
  - aortica/io/wfdb_reader.py (new) - read_wfdb() with unit normalization (mV→µV), multi-segment support via m2s=True
  - aortica/io/__init__.py (modified) - exports read_wfdb and ECGRecord
  - tests/io/test_wfdb_reader.py (new) - 15 fast tests (synthetic WFDB fixtures) + 3 slow PhysioNet integration tests (MIT-BIH 100, PTB-XL 00001_hr)
- Learnings for future iterations:
  - wfdb.rdrecord() with m2s=True automatically flattens multi-segment records into a single Record
  - wfdb returns signals as [samples, channels] — must transpose to [leads, samples] for ECGRecord
  - wfdb records store physical units per-channel; reader normalizes all to µV using a lookup table
  - wfdb.wrsamp() can create synthetic WFDB records for testing without needing real data files
  - wfdb.dl_database() can download specific records from PhysioNet for integration tests
---

## Iteration 5 - US-005: CSV and MAT Format Readers
- Implemented: `read_csv()` with `CSVConfig` and `read_mat()` with `MATConfig`, both returning ECGRecord
- Files changed:
  - aortica/io/csv_reader.py (new) - read_csv() with CSVConfig dataclass, supports column-per-lead and row-per-lead orientations, time column exclusion, custom delimiter, skip rows
  - aortica/io/mat_reader.py (new) - read_mat() with MATConfig dataclass, supports leads_first/samples_first orientation, optional lead names and sample rate variables from MAT file
  - aortica/io/__init__.py (modified) - exports read_csv, CSVConfig, read_mat, MATConfig
  - tests/io/test_csv_mat_readers.py (new) - 27 tests covering both readers: basic loading, orientations, metadata, error handling, ECGRecord integration
- Learnings for future iterations:
  - scipy.io.loadmat is already available as scipy is a core dep — no new dependency needed for MAT files
  - numpy.genfromtxt is simpler than pandas for pure-numeric CSV reading
  - MATLAB cell arrays of strings come back as nested object ndarrays from loadmat — _extract_lead_names helper handles the various nested forms
  - CSVConfig and MATConfig use dataclasses with sensible defaults for clean API
  - Both readers follow the same pattern as wfdb_reader.py: validate path, load data, normalize to ECGRecord
---

## Iteration 6 - US-006: DICOM and SCP-ECG Format Readers
- Implemented: `read_dicom()` for DICOM ECG waveform files (Supplement 30/130) and `read_scp()` for SCP-ECG binary format files
- Files changed:
  - aortica/io/dicom_reader.py (new) - read_dicom() using pydicom, extracts waveform data, channel definitions, patient demographics
  - aortica/io/scp_reader.py (new) - read_scp() with minimal SCP-ECG binary parser, handles Sections 0/1/3/6
  - aortica/io/__init__.py (modified) - exports read_dicom and read_scp
  - tests/io/test_dicom_scp_readers.py (new) - 18 tests with synthetic DICOM and SCP-ECG file generators
- Learnings for future iterations:
  - pydicom was already a core dependency in pyproject.toml — no new dep needed
  - DICOM ECG waveform data is in WaveformSequence[0], channels are interleaved [sample0_ch0, sample0_ch1, ...]
  - Channel sensitivity & correction factor must both be applied: physical = digital * sensitivity * correction + baseline
  - SCP-ECG is a binary format with numbered sections; Section 0 has pointers, Section 1 has patient data, Section 3 has lead definitions, Section 6 has waveform data
  - SCP-ECG section offsets in Section 0 are 1-based byte offsets from start of record
  - SCP-ECG AVM (amplitude value multiplier) is stored in nanovolts per LSB — divide by 1000 for µV
  - SCP-ECG lead ID codes map to standard lead names (1=I, 2=II, 3=V1, etc.) per AHA conventions
  - Synthetic file generators in tests are invaluable for testing binary format parsers without real data
  - ruff auto-fix handles import sorting issues from the `typing.Any` import in scp_reader.py
---

## Iteration 7 - US-007: HL7 aECG XML Format Reader
- Implemented: `aortica.io.read_hl7_aecg(path)` for HL7 aECG (FDA XML) ECG files
- Files changed:
  - aortica/io/hl7_aecg_reader.py (new) - read_hl7_aecg() using xml.etree.ElementTree, HL7 v3 namespace, lead codes mapping, scale/origin conversion, demographics extraction
  - aortica/io/__init__.py (modified) - exports read_hl7_aecg
  - tests/io/test_hl7_aecg_reader.py (new) - 16 tests with synthetic HL7 aECG XML builder
  - PRD.md (modified) - marked US-007 complete
- Learnings for future iterations:
  - HL7 aECG uses the urn:hl7-org:v3 XML namespace — all element lookups need namespace-aware find/findall
  - Waveform data is stored as space-separated digit strings in <digits> elements, with <scale> and <origin> for physical unit conversion
  - Sample rate is determined from the <increment> element in the time-base sequence (value + unit)
  - Lead codes use MDC_ECG_LEAD_* naming convention that maps to standard lead names (I, II, V1, etc.)
  - Structure: AnnotatedECG → component → series → sequenceSet → component(s) → sequence
  - No additional dependencies needed — xml.etree.ElementTree is in the Python stdlib
  - Demographics are at various XPath depths depending on the file; need to try multiple paths
---


## Iteration 8 - US-008: Universal Format Dispatcher
- Implemented: `aortica.io.read_ecg(path)` universal format dispatcher with auto-detection, lead normalization, and resampling
- Files changed:
  - aortica/io/dispatcher.py (new) - read_ecg() with extension + magic-byte format detection, _normalise_leads for 12-lead standard ordering, UnsupportedFormatError, configurable resampling
  - aortica/io/__init__.py (modified) - exports read_ecg, UnsupportedFormatError, STANDARD_12_LEAD_ORDER
  - tests/io/test_dispatcher.py (new) - 35 tests covering: magic-byte sniffing (DICOM, SCP, HL7/XML), extension dispatch, explicit format parameter, 12-lead normalization (shuffled, case-insensitive, non-12-lead passthrough), resampling, full pipeline, edge cases
  - aortica/io/ecg_record.py (bugfix) - fixed missing @ on @dataclass decorator and double triple-quote syntax error in to_millivolts docstring
  - PRD.md (modified) - marked US-008 complete
- Learnings for future iterations:
  - CSVConfig and MATConfig both require sample_rate as a positional arg — read_ecg raises ValueError if csv_config/mat_config not provided for those formats
  - DICOM magic bytes are at offset 128-131 ("DICM"); SCP-ECG can be heuristically detected by CRC+record_length+section_id=0 pattern
  - HL7 aECG XML is detected by checking for "urn:hl7-org:v3" or "AnnotatedECG" in the first 512 bytes
  - Lead normalization uses case-insensitive matching but outputs standard casing (I, II, III, aVR, aVL, aVF, V1-V6)
  - WFDB can be referenced by base path (no extension) — dispatcher checks for .hea sibling file
  - Pre-existing bugs found and fixed in ecg_record.py: missing @ on @dataclass, and """""" instead of """ closing docstring
---

## Iteration 9 - US-009: QRS Detection Module
- Implemented: `aortica.signal.detect_qrs(ecg_record, method, lead)` with two backends
- Files changed:
  - aortica/signal/qrs_detection.py (new) - detect_qrs() with NeuroKit2 and Pan-Tompkins backends, lead selection, sorted R-peak indices
  - aortica/signal/__init__.py (modified) - exports detect_qrs
  - tests/signal/test_qrs_detection.py (new) - 19 tests covering both backends, edge cases (high/low HR, short signal, fallback lead), dispatch errors
  - PRD.md (modified) - marked US-009 complete
- Learnings for future iterations:
  - neurokit2 is optional ([signal] extra) and not installed in the dev environment — tests use pytest.importorskip to gracefully skip
  - Pan-Tompkins backend is pure SciPy: bandpass (5-15 Hz) → derivative → squaring → moving-window integration → find_peaks
  - mypy complains about `no-any-return` from ndarray indexing — assign to typed local variable first
  - _select_lead() defaults to Lead II if available, else first lead — consistent with clinical convention
  - Synthetic ECG fixture uses Gaussian-shaped QRS complexes at fixed HR with baseline wander — good enough for testing peak detection
  - ruff auto-fix (`ruff check --fix`) handles import sorting for numpy.typing.NDArray addition
---

## Iteration 10 - US-010: Signal Denoising Module
- Implemented: `aortica.signal.denoise(ecg_record, methods)` with three configurable filter stages
- Files changed:
  - aortica/signal/denoising.py (new) - denoise() with baseline wander (highpass Butterworth), powerline (notch filter with auto-detect 50/60 Hz), high-frequency noise (lowpass Butterworth); all zero-phase via sosfiltfilt/filtfilt
  - aortica/signal/__init__.py (modified) - exports denoise
  - tests/signal/test_denoising.py (new) - 23 tests: baseline removal, powerline 50/60 Hz, auto-detection, highfreq removal, combined pipeline, SNR verification (>6 dB improvement), edge cases (low sample rate, above-Nyquist), metadata preservation
  - PRD.md (modified) - marked US-010 complete
- Learnings for future iterations:
  - scipy.signal.butter + sosfiltfilt provides zero-phase IIR filtering — no phase distortion on the ECG waveform
  - scipy.signal.iirnotch + filtfilt for powerline removal; Q=30 gives a narrow notch that doesn't affect adjacent frequencies
  - Auto-detection of 50/60 Hz powerline works by comparing FFT spectral peaks around each candidate frequency
  - Filters degrade gracefully when cutoff exceeds Nyquist (silently skip rather than crash)
  - ruff auto-fix handles unused imports and import sorting
---

## Iteration 11 - US-011: Signal Quality Scoring Module
- Implemented: `aortica.signal.score_quality(ecg_record)` with per-lead quality scoring and `QualityReport`
- Files changed:
  - aortica/signal/quality_scoring.py (new) - score_quality(), QualityReport, LeadQuality dataclasses; detects flatline, clipping, baseline wander, motion artifact via FFT spectral analysis and signal statistics
  - aortica/signal/__init__.py (modified) - exports score_quality, QualityReport, LeadQuality
  - tests/signal/test_quality_scoring.py (new) - 26 tests covering API, clean signals, flatline, clipping, baseline wander, motion artifact, classification thresholds, multi-lead, edge cases
  - PRD.md (modified) - marked US-011 complete
- Learnings for future iterations:
  - Penalty-based scoring: each artefact deducts from 100 (flatline=40, clipping=30, wander=20, motion=20); max penalty caps at 100
  - Flatline detection uses np.diff with absolute tolerance — catches lead-off and constant-value segments
  - Clipping detection uses samples near signal min/max rail based on the 99.5th percentile approach
  - Baseline wander uses FFT spectral energy ratio below 0.5 Hz vs total; motion artifact uses energy above 40 Hz vs total
  - Motion artifact detector gracefully skips when Nyquist < 40 Hz (low sample rates)
  - A single flatline penalty (40) yields score 60 = 'marginal', not 'poor'; need multiple artefacts to reach 'poor' (<40)
  - QualityReport.recommendation maps directly: good→accept, marginal→review, poor→reject
---

## Iteration 12 - US-012: PTB-XL Dataset Loader
- Implemented: `aortica.data.load_ptbxl(path, sampling_rate=500)` returning `train/val/test` tuples (`records, labels`).
- Files changed:
  - aortica/data/ptbxl.py (new) - load_ptbxl(), _parse_scp_statements()
  - aortica/data/__init__.py (modified) - exports load_ptbxl
  - tests/data/test_ptbxl.py (new) - tests with synthetic scp_statements.csv, ptbxl_database.csv, and wfdb files
  - PRD.md (modified) - marked US-012 complete
- Learnings for future iterations:
  - `load_ptbxl` eager loads the dataset by iterating `ptbxl_database.csv` and calling `read_ecg()`.
  - The universal format dispatcher `read_ecg(path, target_rate=X)` implicitly resamples, meaning 100 Hz files request resample cleanly.
  - Using `ast.literal_eval` to parse `scp_codes` dict strings from PTB-XL metadata is robust.
  - `scp_statements.csv`'s index corresponds to the keys in `scp_codes` dict. 
---

## Iteration 13 - US-013: ECG Dataset and DataLoader Utilities
- Implemented: PyTorch and TensorFlow dataset wrappers for ECG data along with augmentation utilities.
- Files changed:
  - aortica/data/augmentations.py (new) - apply_lead_dropout, apply_gaussian_noise, apply_time_shift, apply_amplitude_scale, pad_or_truncate
  - aortica/data/dataset.py (new) - ECGDataset (for torch), create_tf_dataset (for tf)
  - aortica/data/__init__.py (modified) - exports ECGDataset, create_tf_dataset
  - tests/data/test_augmentations.py (new) - tests for augmentation functions
  - tests/data/test_dataset.py (new) - tests for PyTorch and TF datasets (skipped gracefully if uninstalled)
  - tests/data/test_ptbxl.py (modified) - added type hints to existing tests fixing mypy errors
  - PRD.md (modified) - marked US-013 complete
- Learnings for future iterations:
  - Dataset augmentations operate on NumPy arrays before tensor conversion, making it easier to share logic between PyTorch (ECGDataset) and TensorFlow (tf.data via generator).
  - PyTorch Dataset subclassing handles missing torch dependency gracefully with a dummy base class for typing purposes.
  - tf.data.Dataset.from_generator is effective for yielding from memory records while allowing parallel downstream ops.
---

## Iteration 14 - US-014: Baseline Rhythm Classification Model (PTB-XL)
- Implemented: 1D ResNet-18 model and training script for PTB-XL rhythm classification
- Files changed:
  - aortica/models/resnet1d.py (new) - ResNet1D with ResidualBlock1D, 4 stages [64,128,256,512], adaptive pooling, sigmoid output
  - aortica/models/train_baseline.py (new) - train() function with CLI, configurable hyperparameters, set_seed() for reproducibility, per-epoch F1/loss tracking, best checkpoint saving, JSON metrics export
  - aortica/models/__init__.py (modified) - exports ResNet1D
  - tests/models/test_baseline.py (new) - 20 tests covering: ResidualBlock1D shapes, ResNet1D output shapes/range/variable-length/gradient/param-count/reproducibility, F1 computation (perfect/wrong/partial), training utils (seed, train_one_epoch, evaluate, checkpoint save/load, metrics JSON)
  - PRD.md (modified) - marked US-014 complete
- Learnings for future iterations:
  - When torch is not installed, class definitions inheriting nn.Module fail at import time — provide a dummy Module base class via `types.ModuleType` stubs (same pattern used in dataset.py)
  - ResNet1D uses AdaptiveAvgPool1d(1) so it handles any input signal length without changes
  - `_compute_f1` uses manual TP/FP/FN counting for macro-F1 — no sklearn dependency needed
  - All torch-dependent tests use `pytest.importorskip("torch")` to skip gracefully on Python 3.13
  - ruff auto-fix handles import sorting for noqa: E402 lines after pytest.importorskip
---

## Iteration 15 - US-015: Shared ResNet Backbone Encoder
- Implemented: `AorticaBackbone` (PyTorch) and `build_aortica_backbone_tf()` (TensorFlow/Keras) shared encoder
- Also fixed: US-013 PRD checkboxes were not marked complete in a prior iteration — fixed now
- Files changed:
  - aortica/models/backbone.py (new) - AorticaBackbone with ResidualBlock1D, 3 stages [64, 128, 256], adaptive pooling, configurable feature_dim (default 256)
  - aortica/models/backbone_tf.py (new) - build_aortica_backbone_tf() Keras functional API model, identical architecture, channels-last format
  - aortica/models/__init__.py (modified) - exports AorticaBackbone
  - tests/models/test_backbone.py (new) - 18 tests covering PyTorch (shapes, varying lengths/rates, gradient flow, reproducibility, eval mode, param count) and TF (shapes, variable-length, summary)
  - PRD.md (modified) - marked US-013 and US-015 complete
- Learnings for future iterations:
  - AorticaBackbone uses 3 stages (64, 128, 256) vs ResNet1D's 4 stages (64, 128, 256, 512) — lighter for multi-task sharing
  - feature_dim defaults to 256 (last stage width); optional fc projection layer added only when feature_dim differs
  - TF/Keras uses functional API with channels-last (batch, samples, leads) — caller must transpose from PyTorch's (batch, leads, samples)
  - AdaptiveAvgPool1d(1) in PyTorch / GlobalAveragePooling1D in Keras makes backbone handle any signal length
  - Same torch-optional pattern: _check_torch(), dummy nn.Module stub, pytest.importorskip
---

## Iteration 16 - US-013 Quality Refactor
- Reviewed and rewrote US-013 code for production quality
- Files changed:
  - aortica/data/augmentations.py (rewritten) - migrated np.random.RandomState → np.random.Generator, added input validation (dropout_prob range, negative std_dev, min>max scale, target_samples<=0), empty signal guards, docstring documenting recommended augmentation order
  - aortica/data/dataset.py (rewritten) - migrated to Generator, ensured float32 label dtypes in both PyTorch and TF paths, extracted shared `_apply_augmentations()` helper, added empty records guard for TF, simplified TF output_signature construction
  - tests/data/test_augmentations.py (rewritten) - expanded from 5 tests to 34: organized by class, covers shape preservation, boundary conditions, reproducibility, input validation errors, empty signals
  - tests/data/test_dataset.py (rewritten) - expanded from 2 tests to 15: covers output shapes/dtypes, augmentation effects, reproducibility, windowing, resampling, error cases, scalar labels, TF dataset shapes
- Learnings for future iterations:
  - np.random.Generator uses `rng.integers()` instead of `randint()` — different API from legacy RandomState
  - `.gitignore` has `data/` which catches `aortica/data/` and `tests/data/` — must use `git add -f`
  - `np.asarray(labels, dtype=np.float32)` ensures consistent label dtypes regardless of input type
  - Factoring augmentation logic into a shared helper eliminates code duplication between PyTorch Dataset and TF generator
---

## Iteration 17 - US-016: Cross-Lead Temporal Attention Module
- Implemented: `CrossLeadAttention` (PyTorch) and `build_cross_lead_attention_tf()` (TF/Keras)
- Files changed:
  - aortica/models/attention.py (new) - CrossLeadAttention: multi-head self-attention across ECG leads with extractable attention weights, residual + LayerNorm, configurable num_heads/head_dim/dropout
  - aortica/models/attention_tf.py (new) - build_cross_lead_attention_tf(): Keras functional API model returning (enriched_features, attention_weights)
  - aortica/models/__init__.py (modified) - exports CrossLeadAttention
  - tests/models/test_attention.py (new) - 22 tests: output shapes, attention weight properties (sum-to-1, non-negative, detached), gradient flow, reproducibility, dropout, backbone integration, TF equivalents
  - PRD.md (modified) - marked US-016 complete
- Learnings for future iterations:
  - CrossLeadAttention reshapes backbone features [batch, feature_dim] → [batch, num_leads, lead_token_dim] to create per-lead tokens for attention
  - feature_dim must be divisible by num_leads — enforce with ValueError
  - Attention weights are stored detached from the computation graph for XAI extraction
  - TF/Keras functional API returns a tuple (enriched, attn_weights) as model outputs — different from PyTorch where weights are accessed via property
  - ruff auto-fix handles import sorting (numpy before aortica imports in TF test methods)
  - Same torch-optional pattern: _check_torch(), dummy nn.Module stub, pytest.importorskip
---

## Iteration 18 - US-017: Rhythm & Conduction Task Head (22 Classes)
- Implemented: `RhythmHead` (PyTorch) and `build_rhythm_head_tf()` (TF/Keras) for 22-class multi-label rhythm classification
- Files changed:
  - aortica/models/rhythm_head.py (new) - RhythmHead with forward/forward_logits, compute_rhythm_loss with optional class weights, RHYTHM_CLASSES canonical list
  - aortica/models/rhythm_head_tf.py (new) - build_rhythm_head_tf() Keras functional model with Dense→ReLU→Dropout→Dense→Sigmoid
  - aortica/models/__init__.py (modified) - exports RhythmHead
  - tests/models/test_rhythm_head.py (new) - 18 tests: constants, shapes, ranges, loss computation, gradient flow, backbone+attention integration, TF equivalents
  - PRD.md (modified) - marked US-017 complete
- Learnings for future iterations:
  - RhythmHead exposes both forward() (sigmoid probs) and forward_logits() (raw logits for BCEWithLogitsLoss)
  - compute_rhythm_loss wraps nn.functional.binary_cross_entropy_with_logits with optional per-class weight tensor
  - TF/Keras version uses Activation("sigmoid") as a separate layer for clearer architecture
  - Same torch-optional pattern: _check_torch(), dummy nn.Module stub, pytest.importorskip
---

## Iteration 19 - US-018: Structural & Functional Task Head (15 Classes)
- Implemented: `StructuralHead` (PyTorch) and `build_structural_head_tf()` (TF/Keras) for 15-class multi-label structural classification
- Files changed:
  - aortica/models/structural_head.py (new) - StructuralHead with forward/forward_logits, compute_structural_loss with optional focal loss for rare classes, STRUCTURAL_CLASSES canonical list
  - aortica/models/structural_head_tf.py (new) - build_structural_head_tf() Keras functional model with Dense→ReLU→Dropout→Dense→Sigmoid
  - aortica/models/__init__.py (modified) - exports StructuralHead
  - tests/models/test_structural_head.py (new) - 22 tests: constants, shapes, ranges, BCE loss, focal loss (gamma/alpha/gradient), backbone+attention integration, TF equivalents
  - PRD.md (modified) - marked US-018 complete
- Learnings for future iterations:
  - Focal loss implementation: (1-p_t)^gamma * BCE, where p_t = p for positive and (1-p) for negative class
  - With gamma=0, focal loss reduces exactly to standard BCE — useful sanity check
  - focal_alpha applies per-class balancing: alpha_t for positives, (1-alpha_t) for negatives
  - Same torch-optional pattern: _check_torch(), dummy nn.Module stub, pytest.importorskip
---

## Iteration 20 - US-019: Ischaemia & Metabolic Task Head (10 Classes)
- Implemented: `IschaemiaHead` (PyTorch) and `build_ischaemia_head_tf()` (TF/Keras) for 10-class multi-label ischaemia/metabolic classification
- Files changed:
  - aortica/models/ischaemia_head.py (new) - IschaemiaHead with forward/forward_logits, compute_ischaemia_loss with optional class weights, ISCHAEMIA_CLASSES canonical list
  - aortica/models/ischaemia_head_tf.py (new) - build_ischaemia_head_tf() Keras functional model with Dense→ReLU→Dropout→Dense→Sigmoid
  - aortica/models/__init__.py (modified) - exports IschaemiaHead
  - tests/models/test_ischaemia_head.py (new) - 18 tests: constants, shapes, ranges, loss computation, gradient flow, backbone+attention integration, TF equivalents
  - PRD.md (modified) - marked US-019 complete
- Learnings for future iterations:
  - IschaemiaHead follows exactly the same pattern as RhythmHead (BCE loss with class weights) — no focal loss needed here since class weighting handles it
  - Same torch-optional pattern: _check_torch(), dummy nn.Module stub, pytest.importorskip
---
